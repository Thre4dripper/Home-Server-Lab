# Aria2 Configuration
CONTAINER_NAME=aria2-ui
RESTART_POLICY=unless-stopped

# Port Configuration
WEBUI_PORT=8004

# User Configuration
PUID=1000
PGID=1004

# Aria2 RPC Configuration
RPC_SECRET=your-secret-change-this

# ARIA2RPCPORT: The port that AriaNg UI will use to connect to aria2 RPC
# - For HTTPS reverse proxy: set to 443 (default HTTPS port)
# - For HTTP reverse proxy: set to 80 or your custom port
# - For direct access: set to 6800 (aria2's default RPC port)
# Note: aria2 inside container always runs on port 6800 internally
ARIA2RPCPORT=443

# Optional: Embed RPC secret into web UI (INSECURE - only use with auth)
# Set to true to auto-configure AriaNg with your secret (skip manual entry)
# WARNING: Anyone accessing the web UI can see the secret in the code
EMBED_RPC_SECRET=false

# Paths
DOWNLOADS_PATH=/home/pi/pendrive/downloads
CONFIG_PATH=./config
apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: homelab
  namespace: argocd
spec:
  goTemplate: true
  goTemplateOptions: ["missingkey=error"]
  generators:
    - list:
        elements:
          # ── Infra / Cluster ────────────────────────────────
          - name: priority-classes
            namespace: kube-system
            category: infra
            path: k3s/infra/priority-classes

          # ── Monitoring ─────────────────────────────────────
          - name: dashdot
            namespace: monitoring
            category: monitoring
            path: k3s/apps/dashdot
          - name: portainer
            namespace: monitoring
            category: monitoring
            path: k3s/apps/portainer

          # ── Dashboard / Network ────────────────────────────
          - name: homarr
            namespace: dashboard-network
            category: dashboard-network
            path: k3s/apps/homarr
          - name: pihole
            namespace: dashboard-network
            category: dashboard-network
            path: k3s/apps/pihole
          - name: twingate
            namespace: dashboard-network
            category: dashboard-network
            path: k3s/apps/twingate
          - name: homepage
            namespace: dashboard-network
            category: dashboard-network
            path: k3s/apps/homepage

          # ── Media ──────────────────────────────────────────
          - name: jellyfin
            namespace: media
            category: media
            path: k3s/apps/jellyfin

          # ── File Management ────────────────────────────────
          - name: filebrowser
            namespace: file-management
            category: file-management
            path: k3s/apps/filebrowser
          - name: samba
            namespace: file-management
            category: file-management
            path: k3s/apps/samba

          # ── Automation ─────────────────────────────────────
          - name: home-assistant
            namespace: automation
            category: automation
            path: k3s/apps/home-assistant
          - name: n8n
            namespace: automation
            category: automation
            path: k3s/apps/n8n

          # ── Databases ──────────────────────────────────────
          # NOTE: databases live at k3s/databases/* (sibling of k3s/apps/) since
          # they are stateful infrastructure with different lifecycle from apps.
          - name: mongodb
            namespace: databases
            category: databases
            path: k3s/databases/mongodb
          - name: postgres
            namespace: databases
            category: databases
            path: k3s/databases/postgres
          - name: redis
            namespace: databases
            category: databases
            path: k3s/databases/redis

          # ── Git ────────────────────────────────────────────
          - name: forgejo
            namespace: git
            category: git
            path: k3s/apps/forgejo

          # ── Downloads ──────────────────────────────────────
          - name: aria2
            namespace: downloads
            category: downloads
            path: k3s/apps/aria2
          - name: bitcomet
            namespace: downloads
            category: downloads
            path: k3s/apps/bitcomet

  ignoreApplicationDifferences:
    - jsonPointers:
        - /spec/syncPolicy/automated

  template:
    metadata:
      name: "{{ .name }}"
      namespace: argocd
      labels:
        category: "{{ .category }}"
      finalizers:
        - resources-finalizer.argocd.argoproj.io
    spec:
      project: default
      source:
        repoURL: https://github.com/Thre4dripper/Home-Server-Lab.git
        targetRevision: HEAD
        path: "{{ .path }}"
      destination:
        server: https://kubernetes.default.svc
        namespace: "{{ .namespace }}"
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
          - RespectIgnoreDifferences=true
      ignoreDifferences:
        - group: apps
          kind: Deployment
          jsonPointers:
            - /spec/template/metadata/annotations/kubectl.kubernetes.io~1restartedAt

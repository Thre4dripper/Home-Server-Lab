apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: homarr
  namespace: dashboard-network
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`homarr.lan`)
      kind: Rule
      services:
        - name: homarr
          port: 8100

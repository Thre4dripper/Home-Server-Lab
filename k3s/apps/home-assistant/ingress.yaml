apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: home-assistant
  namespace: automation
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`ha.lan`)
      kind: Rule
      services:
        - name: home-assistant
          port: 8123

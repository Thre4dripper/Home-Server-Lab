apiVersion: v1
kind: Service
metadata:
  name: homepage
  namespace: dashboard-network
  labels:
    app: homepage
spec:
  type: LoadBalancer
  selector:
    app: homepage
  ports:
    - port: 8600
      targetPort: 3000
      protocol: TCP

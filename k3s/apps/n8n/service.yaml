apiVersion: v1
kind: Service
metadata:
  name: n8n
  namespace: automation
spec:
  type: LoadBalancer
  selector:
    app: n8n
  ports:
    - name: web
      port: 8400
      targetPort: 5678
      protocol: TCP

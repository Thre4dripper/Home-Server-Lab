apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: n8n
  namespace: automation
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`n8n.lan`)
      kind: Rule
      services:
        - name: n8n
          port: 8400

apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: pihole
  namespace: dashboard-network
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`pihole.lan`)
      kind: Rule
      services:
        - name: pihole
          port: 8110

# Nextcloud AIO Configuration Example

# Set your domain here (required for SSL certificates)
NEXTCLOUD_DOMAIN=<YOUR_DOMAIN_HERE>

# Server IP (auto-detected by setup.sh)
PI_IP=<YOUR_SERVER_IP>

# Timezone
TZ=<YOUR_TIMEZONE>

# External Storage Path (like in pydio setup)
EXTERNAL_STORAGE_PATH=<PATH_TO_EXTERNAL_STORAGE>

# Optional: Disable backup section in AIO interface
# AIO_DISABLE_BACKUP_SECTION=false

# Optional: Adjust Talk port (default: 3478)
# TALK_PORT=3478

# Optional: Borg backup retention policy
# BORG_RETENTION_POLICY=--keep-within=7d --keep-weekly=4 --keep-monthly=6

# Optional: Nextcloud upload limit (default: 16G)
# NEXTCLOUD_UPLOAD_LIMIT=16G

# Optional: PHP memory limit (default: 512M)
# NEXTCLOUD_MEMORY_LIMIT=512M

# Optional: Max execution time (default: 3600)
# NEXTCLOUD_MAX_TIME=3600
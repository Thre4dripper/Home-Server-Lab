# GitLab Community Edition Configuration Example
# Copy this file to .env and customize the values

# Network Configuration
GITLAB_DOMAIN=192.168.0.108
GITLAB_HOSTNAME=gitlab.local
GITLAB_PORT=8929
GITLAB_HTTP_PORT=8929
GITLAB_SSH_PORT=2424

# Admin Configuration (if password is not within security policies gitlab will reject it)
GITLAB_ROOT_PASSWORD=change_this_secure_password

# Application Configuration
GITLAB_SHARED_RUNNERS_REGISTRATION_TOKEN=change_this_token
GITLAB_BACKUP_SCHEDULE="0 2 * * *"
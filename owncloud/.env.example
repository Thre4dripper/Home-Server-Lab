# ownCloud Configuration Template
# Copy this file to .env and update with your actual values

# ownCloud Version
OWNCLOUD_VERSION=10.15

# Network Configuration
OWNCLOUD_DOMAIN=your-server-ip:8080
OWNCLOUD_TRUSTED_DOMAINS=your-server-ip:8080,your-server-ip,localhost:8080,localhost
HTTP_PORT=8080

# Admin User
ADMIN_USERNAME=admin
ADMIN_PASSWORD=secure_admin_password

# Database Configuration
DB_PASSWORD=secure_db_password
DB_ROOT_PASSWORD=secure_root_password

# Security Notes:
# 1. Change all passwords to strong, unique values
# 2. Replace 'your-server-ip' with your actual server IP address
# 3. Add additional domains/IPs to OWNCLOUD_TRUSTED_DOMAINS as needed
# 4. Never commit the actual .env file to git
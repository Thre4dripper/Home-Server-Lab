# FileBrowser Configuration
FILEBROWSER_TAG=s6
CONTAINER_NAME=filebrowser
RESTART_POLICY=unless-stopped

# Network Configuration
FILEBROWSER_PORT=8080

# User Configuration
TIMEZONE=UTC
PUID=1000
PGID=1000

# Root directory to browse
# Default: /home/pi (entire home directory)
# Examples:
#   SRV_PATH=/home/pi/media  (only media files)
#   SRV_PATH=/  (entire filesystem - use with caution)
SRV_PATH=/home/pi

# Resource Limits
MEMORY_LIMIT=256M
MEMORY_RESERVATION=64M
